const Wordmark = () => (
  <h1 className="font-playfair text-xl font-semibold tracking-[0.25em] text-foreground uppercase text-center select-none">
    Touchstone
  </h1>
);

export default Wordmark;
